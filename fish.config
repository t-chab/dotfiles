# emacs ansi-term support
if test -n "$EMACS"
  set -x TERM eterm-color
end

# this function may be required
function fish_title
  true
end

if status --is-interactive
  keychain --eval --quiet -Q id_rsa id_centauri id_pre id_essai id_if id_simu
  source ~/.keychain/(echo $HOSTNAME)-fish
end

